import { createStyles } from "@mantine/core";

const EventSelectStyles = createStyles((theme) => ({
    root: {
      position: 'relative',
    },
  
    input: {
      height: 'auto',
    },

    
  }));

export default EventSelectStyles